# StudyHub 课管助手 PRD v3.0

> **产品需求文档 · 第三版**
>
> 面向大学生的国风课程管理与学习陪伴工具

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-22 | 初稿：课表、月历、Todo、自习室、设置五大模块 |
| v2.0 | 2026-08-23 | 重大迭代：周视图课表、名言警句、极简Todo/DDL双列、月历双点标记、自定义番茄钟、线上自习室、聊天室 |
| v3.0 | 2026-08-23 | 国风视觉重构、便签功能、小组件自定义、法定节假日、番茄钟时长记录与月报/学期总结、BGM接入、名言三模式+名言便签库、星星按钮学期规划 |

---

## 1. 需求背景

### 1.1 业务触发源

本产品定位为小红书国风工具赛道的参赛作品。大学生群体每学期面临课表管理、作业Deadline追踪、自习安排等多重需求，市面现有工具功能臃肿、广告泛滥，缺少集成性方案。v3.0在此基础上全面引入淡雅国风视觉风格，既满足实用功能，又承载文化表达，兼顾"好看"与"好用"。

### 1.2 用户痛点

**视觉与情感：**
- 市面课表/效率工具同质化严重，缺少文化辨识度和情绪价值
- 工具应用普遍冰冷，缺少书卷气和仪式感
- 个性化程度不足，用户无法按自己喜好排列小组件、添加装饰元素

**功能断层：**
- 课表不显示法定节假日，假期安排需要额外查日历
- 课表时间栏固定，无法适配各校不同的作息时间
- 番茄钟不记录专注数据，学了多久全凭感觉
- 进入线上自习室后番茄钟中断，无法连续记录"做了什么"
- 自习室没有BGM，安静到尴尬或需要额外开音乐App
- 没有便签随手记功能，临时灵感无处安放
- 名言警句只能固定一句，缺少变化和个性化
- 学期末没有回顾总结，努力过程不可视

### 1.3 需求目标

- **国风视觉**：宣纸米白 + 黛青朱砂的淡雅国风，书卷气而不失现代易用性
- **数据驱动**：番茄钟专注时长、自习室时长、Todo/DDL完成率全程记录
- **深度个性化**：小组件可自由添加、移动、装饰，打造专属学习桌面
- **学习回顾**：月报激励 + 学期末交互式总结，让努力被看见
- **陪伴不打扰**：线上自习室提供BGM与陪伴感，聊天延迟接收不干扰专注

---

## 2. 用户画像与核心场景

### 2.1 目标用户

| 用户类型 | 特征 | 核心需求 | 使用频率 |
|----------|------|----------|----------|
| 学霸型 | 目标明确，每天固定自习 | 番茄钟定制+时长记录、月报复盘、学期总结 | 每日 |
| 普通型 | 上课+作业+偶尔突击 | 课表查看、DDL提醒、便签速记 | 每周3-5次 |
| 摸鱼型 | 拖延症严重 | DDL预警、线上自习监督、BGM陪伴 | 每周1-2次 |
| 国风爱好者 | 注重审美与文化氛围 | 国风UI、名言便签收集、个性化装饰 | 每日 |

### 2.2 核心使用场景

**场景一：早起看课表+写便签**
> 小明打开App，顶部印章"庚寅年秋·第三周"映入眼帘，名言栏今日随机展示了一句苏轼的词。往下看周课表，周四标着"清明节"——放假了。课表下方便签上记着昨晚写的"记得带实验报告"。再往下看Todo和DDL，心里有数了。

**场景二：课间速记+学期规划**
> 高数课老师布置了大作业，小明点开主页栏的星星按钮，看了看自己开学初写的学期规划——"这学期高数要到85+"，然后在便签上记下"高数大作业 P56-60"。

**场景三：晚间番茄钟+时长记录**
> 晚上9点，小明打开自习栏，创建了一个"高数复习 50min"番茄钟并开始计时。计时结束后，系统自动记录：今日高数类专注50分钟，今日总专注120分钟。月底自动生成月报。

**场景四：线上自习室+BGM**
> 周末小明加入"考研自习室"，打开番茄钟继续计时，同时接入网易云音乐的"专注白噪音"歌单做BGM。聊天框收着不打扰，专注了两个小时，系统记录了自习室时长。

**场景五：学期末回顾**
> 学期结束，App弹出交互式总结页面："你过去的一学期，有50天学习到深夜"——小明点击屏幕，下一句："完成了32个Todo，12个DDL"——再点击——"番茄钟总专注时长186小时，高数类占比最高"——最后一句："你定的学期目标是高数85+，恭喜你做到了。"

---

## 3. 产品架构

### 3.1 信息架构

```
StudyHub 课管助手
├── 主页栏（Tab 1，原"课表"）
│   ├── 印章头部（干支纪年 + 周次）
│   ├── 星星按钮 → 学期规划
│   ├── 名言警句（三种模式 + 名言便签库联动）
│   ├── 便签（手写式自由书写）
│   ├── 周视图课表（法定节假日 + 可改动时间栏 + 自主添加课时）
│   └── 底部双列：Todo List / DDL List
├── 待办栏（Tab 2，月历+任务管理）
│   ├── 月份导航
│   ├── 日历网格（双点标记 + DDL名称 + 节假日）
│   └── 日期详情（Todo/DDL分区）
├── 自习栏（Tab 3）
│   ├── 国风主题切换
│   ├── 番茄钟组件（6格滑窗 + 专注时长记录）
│   └── 线上自习室（创建/加入/BGM接入/聊天室+内置番茄钟）
├── 小组件自定义系统（长按进入编辑模式）
└── 我的（从头部入口进入）
    ├── 个人信息
    ├── 名言便签库（收藏的名言警句）
    ├── 学习记录与报告（月报/学期总结）
    ├── 提醒设置
    ├── 课表管理
    └── 其他设置
```

### 3.2 核心设计原则

1. **国风为骨，极简为魂**：传统色彩与书法字体搭配现代卡片布局，不做繁复装饰
2. **一屏效率**：主页看完课表→便签→Todo→DDL，零切换
3. **数据留痕**：每一次专注、每一次完成都自动记录，月报和学期总结有据可依
4. **不打扰原则**：聊天延迟接收，BGM自主选择，专注时不被打断
5. **深度可定制**：小组件自由排列，装饰性元素随心添加

---

## 4. 国风视觉设计系统

> 本章定义全局视觉规范，所有页面统一遵循。用户自定义小组件时也以此为基底。

### 4.1 色彩体系

| 色彩角色 | 色值 | 用途 |
|----------|------|------|
| 宣纸米白 | #F5F0E8 | 全局背景，模拟宣纸底色 |
| 卡片白 | #FDFBF7 | 卡片背景，比底色略亮 |
| 黛青 | #2D5F5D | 主色调，标题、主按钮、选中态 |
| 黛青浅 | #E0EDEB | 主色浅色背景、选中行底色 |
| 朱砂红 | #C03C2F | 点缀色，紧急标记、印章、DDL标识 |
| 朱砂浅 | #FBEAE7 | 紧急任务红条底色 |
| 墨黑 | #1A1A1A | 主文字色（非纯黑） |
| 墨灰 | #6B6B6B | 次要文字色 |
| 淡墨 | #9B9B9B | 辅助文字、占位符 |
| 茶色边框 | #D4C4B0 | 卡片细线边框 |
| 竹青 | #6B8E6B | Todo完成态、待办绿点 |
| 藤黄 | #D4A574 | 中等优先级、装饰元素 |

### 4.2 字体规范

| 用途 | 字体 | 字重 | 说明 |
|------|------|------|------|
| 标题/印章 | 思源宋体 / 方正清刻本悦宋 | Bold | 宋体营造书卷气，用于栏目标题、印章文字 |
| 正文/数据 | 思源黑体 / 苹方 | Regular/Medium | 黑体保证正文阅读清晰度，用于课程名、时间、列表项 |
| 装饰文字 | 方正小篆 / 篆书字体 | — | 仅用于印章图标和干支纪年 |

### 4.3 卡片与容器

- **边框**：1px solid 茶色边框（#D4C4B0），细线感，不使用厚重阴影
- **圆角**：8-12px，柔和但不夸张
- **阴影**：极轻或无，`box-shadow: 0 1px 3px rgba(180,160,130,0.08)`
- **卡片间距**：12px
- **内边距**：16px

### 4.4 课程卡片竖色条

课程卡片左侧有3px宽的竖色条，用于区分不同课程：

| 色条索引 | 颜色 | 色值 | 适用课程举例 |
|----------|------|------|-------------|
| 0 | 黛青 | #2D5F5D | 高等数学 |
| 1 | 朱砂 | #C03C2F | 大学英语 |
| 2 | 竹青 | #6B8E6B | 数据结构 |
| 3 | 藤黄 | #D4A574 | 中国近代史 |
| 4 | 青瓷 | #7BA7A2 | 体育 |
| 5 | 胭脂 | #B5495B | 思政课 |
| 6 | 靛蓝 | #3B5B7E | 物理 |
| 7 | 赭石 | #A0522D | 选修课 |

### 4.5 印章与干支纪年

**印章图标**：
- 朱砂红方形印章，篆书"课管"二字
- 位于主页顶部左侧，24×24px
- 作为App品牌标识，全局唯一

**干支纪年头部**：
- 格式："庚寅年秋·第三周"
- 干支年根据当前年份的天干地支自动计算
- 季节根据当前月份判断：春（2-4月）、夏（5-7月）、秋（8-10月）、冬（11-1月）
- 周次根据学期开始日期计算
- 印章图标 + 干支纪年文字横排，位于状态栏下方

### 4.6 紧急任务朱批红条

DDL列表中，距截止日期 ≤ 3天的DDL项，在列表项左侧添加朱砂红竖条（3px宽），底色为朱砂浅（#FBEAE7），文字保持正常。视觉上模拟古代朱批批注的紧急感。

---

## 5. 功能需求详情

### 5.1 主页栏（Tab 1）

主页栏是App启动默认页，原"课表"栏更名而来。从上到下分为六个区域：印章头部、星星按钮、名言警句、便签、周视图课表、底部Todo/DDL双列。

#### 5.1.1 印章头部与干支纪年

**位置**：状态栏下方，全宽。

**布局**：
- 左侧：朱砂方形印章图标（篆书"课管"）
- 中间：干支纪年文字（宋体），如"庚寅年秋·第三周"
- 右侧：个人头像（圆形，点击进入"我的"页）

**业务逻辑**：
- 干支年由系统根据当前公历年份自动推算天干地支
- 季节和周次根据系统时间和用户设置的学期起始日期计算
- 用户在"我的"页可设置学期开始日期，影响周次计算

---

#### 5.1.2 星星按钮与学期规划

**位置**：名言警句横幅上方，右上角位置。

**布局**：
- 一个五角星图标（⭐），描边样式，黛青色
- 点击后从右侧滑入全屏覆盖的学期规划页

**学期规划页**：
- 全屏宣纸米白背景
- 顶部标题："我的学期之约"（宋体）
- 内容区为可编辑文本区域，用户自由书写学期目标
  - 示例："这学期高数要到85+，过英语六级，读完3本专业书"
- 底部"保存"按钮
- 保存后数据持久化，随时可修改
- 学期末总结时，此处内容作为"目标实现"对比依据

**字段规则**：

| 字段 | 类型 | 必填 | 长度限制 | 默认值 |
|------|------|------|----------|--------|
| 规划内容 | 长文本 | 否 | 1-500字符 | 空（显示引导文案"写下你这学期的目标..."）|

---

#### 5.1.3 名言警句（三种模式）

**位置**：星星按钮下方，全宽横幅。

**视觉样式**（国风化）：
- 背景：宣纸米白卡片，茶色细线边框
- 文字：宋体，居中，墨黑色
- 右下角：作者/出处（墨灰小字）
- 右上角：大号半透明引号装饰（茶色）
- 点击横幅底部弹出模式切换和编辑面板

**三种模式**：

| 模式 | 说明 | 数据来源 |
|------|------|----------|
| 固定名言 | 用户自定义输入一句，每天展示相同内容 | 用户输入 |
| 随机名言 | 每天从工具内置名言库随机抽取一句 | 系统名言库（预置200+条古今中外名言） |
| 个人名言 | 每天从用户自己的名言便签库随机抽取一句 | 用户名言便签库（需先收集） |

**切换逻辑**：
- 点击名言横幅弹出底部面板
- 面板顶部为三选一模式切换（Segmented Control样式）
- 选中"固定名言"时：下方显示名言内容和作者的输入框
- 选中"随机名言"时：下方显示"今日名言来自系统库" + 预览今日随机内容
- 选中"个人名言"时：下方显示"今日名言来自你的便签库" + 预览今日随机内容 + 若便签库为空则提示"请先在名言便签中收集名言"
- 模式切换即时生效，名言栏立即更新

**随机刷新规则**：
- 每日凌晨0:00自动刷新当日名言
- 同一天内不重复刷新（除非用户手动切换模式）
- 随机算法避免短期内重复（最近7天不重复）

---

#### 5.1.4 名言便签库

**入口**：主页名言横幅长按 / "我的"页 → "名言便签库"。

**功能**：
- 用户将看电影、读书籍等作品时遇到的名言警句收集记录
- 每条记录包含：名言内容、来源（书名/电影名/人名）、分类标签

**便签库页面**：
- 顶部搜索框
- 按分类标签分组展示（文学、电影、哲学、自创等）
- 每条便签为小卡片样式，宣纸质感
- 支持添加、编辑、删除
- 点击某条可"设为今日名言"（仅个人名言模式下生效）

**字段规则**：

| 字段 | 类型 | 必填 | 长度限制 |
|------|------|------|----------|
| 名言内容 | 文本 | 是 | 1-100字符 |
| 来源 | 文本 | 否 | 1-30字符 |
| 分类标签 | 选择/自建 | 否 | 预设分类 + 自定义 |

---

#### 5.1.5 便签功能

**位置**：名言警句横幅下方，Todo/DDL列表上方。

**视觉设计**：
- 模拟现实中的黄色便签纸
- 尺寸约为全宽 × 120px
- 背景：淡黄色（#FFF8E1），带轻微纸张纹理
- 左上角有"折角"装饰（模拟便签纸撕角）
- 文字为手写体风格字体（如方正字迹系列），墨黑色
- 底部有一条淡色虚线分隔

**业务逻辑**：
- 首次进入显示空白便签 + 引导提示"点此书写"
- 点击便签任意位置进入编辑状态
- 编辑状态：便签内出现光标，用户可自由书写文字（不限格式、不限行数，超出高度自动滚动）
- 书写内容实时保存（每输入一个字符即保存到本地）
- 退出编辑（点击便签外区域）后，便签显示已书写内容
- 便签内容不与Todo/DDL联动，纯粹是自由记录区
- 便签右上角有清空按钮（小号 × 图标），点击后二次确认清空

**字段规则**：

| 字段 | 类型 | 必填 | 长度限制 | 默认值 |
|------|------|------|----------|--------|
| 便签内容 | 文本 | 否 | 1-500字符 | 空 |

---

#### 5.1.6 周视图课表

**位置**：便签下方，占据页面中部核心区域。

**布局结构**：
- 顶部为星期标题行：左侧时间列占位，右侧7列分别为周一至周日
- 每列标题分两行：上行"周X"，下行日期数字
- 今日对应的列：日期数字用黛青色高亮
- **法定节假日标注**：如该日为法定节假日，在日期数字下方显示节日名称（如"清明"），朱砂色小字
- 左侧为时间列：显示节次编号和开始时间
- 右侧7列×N行的课程网格

**法定节假日显示规则**：
- 数据来源：内置中国法定节假日数据（元旦、春节、清明、劳动节、端午、中秋、国庆等）
- 显示位置：日期数字正下方，节日名称（朱砂红色，约8px）
- 显示格式：如"周四 04 清明"——"周四"为星期，"04"为日期，"清明"为节日名
- 节假日当天课表格子变灰，提示"假日"（用户仍可手动添加课程）

**时间栏可改动**：
- 左侧时间列支持编辑
- 点击时间列的任意节次，弹出编辑表单
- 可修改该节次的开始时间和结束时间
- 修改后所有该节次的课程卡片自动对齐到新时间
- 修改不影响已有课程数据（只改时间栏定义）

**自主添加课时**：
- 时间列底部有"添加课时"按钮（+ 图标）
- 点击后在时间列末尾追加一个新节次
- 新节次需填写：节次名称（如"晚课"）、开始时间、结束时间
- 自主添加的课时与导入课表数据独立存储，不冲突
- 新增课时后，课表网格自动扩展对应行数

**课程卡片**：
- 卡片左侧有3px竖色条（见4.4节色条定义）
- 卡片内容：课程名称（黑体加粗）+ 上课地点（小字）
- 卡片底色为卡片白（#FDFBF7），细线边框
- 跨节课程卡片高度随节数变化
- 点击卡片弹出编辑表单

**编辑模式**：
- 右上角"编辑"按钮进入编辑模式
- 空白格子显示"+"号
- 点击空白添加课程，点击课程编辑/删除

**课程表单字段**：

| 字段 | 类型 | 必填 | 约束 | 默认 |
|------|------|------|------|------|
| 课程名称 | 文本 | 是 | 1-20字符 | 无 |
| 上课地点 | 文本 | 否 | 1-30字符 | 无 |
| 上课星期 | 单选 | 是 | 周一至周日 | 选中格子对应星期 |
| 起始节次 | 单选 | 是 | 第1-N节 | 选中格子对应节次 |
| 持续节数 | 数字 | 是 | 1-4节 | 1 |
| 色条颜色 | 选择 | 否 | 8种预设 | 自动分配 |

---

#### 5.1.7 底部极简双列表

**位置**：周课表下方，左右两列并排。

**左侧：待办列表（TodoList）**

标题栏：竹青色小点 + "待办" + 右侧"+"。

列表项规则：
- 每行结构：左侧空心括号 `[ ]` + 待办文字
- 点击括号变为 ✅ + 删除线 → 完成
- 待办按创建时间倒序排列
- 最多显示5条

添加Todo：输入内容 + 可选日期（月/日格式）。添加后月历出现绿点。

**右侧：DDL列表**

标题栏：朱砂色小点 + "DDL" + 右侧"+"。

列表项规则：
- 每行结构：DDL名称 + 右侧日期标签（如"09/03"）
- 按截止日期升序排列
- **距截止 ≤ 3天的DDL**：左侧出现朱砂红竖条 + 底色为朱砂浅，模拟朱批紧急感
- 已过期DDL：灰色 + 删除线

添加DDL：输入名称 + 必填日期（月/日格式，如09/3）。添加后月历出现红点 + DDL名称。

---

### 5.2 待办栏（Tab 2，月历+任务管理）

待办栏以月历为核心，集中展示Todo和DDL的月度分布。从上到下：月份导航、日历网格、日期详情。

#### 5.2.1 月份导航

- 左侧：当前年月，如"庚寅年 八月"（国风纪月）
- 右侧：左箭头（上月）、"今日"按钮、右箭头（下月）

#### 5.2.2 日历网格

**格子结构**（从上到下）：
1. 日期数字
2. 节假日名称（如有，朱砂小字）
3. DDL名称（朱砂色小字，过长省略）
4. 标记点（竹青绿点 + 朱砂红点）

**日期状态样式**：

| 状态 | 样式 |
|------|------|
| 普通 | 墨黑数字，宣纸米白底 |
| 今日 | 黛青圆形填充，白色数字 |
| 选中 | 黛青浅底色 + 黛青描边 |
| 非本月 | 淡墨色数字 |
| 法定节假日 | 日期数字下方显示节日名（朱砂色） |

**双点标记系统**：

| 标记 | 颜色 | 尺寸 | 含义 |
|------|------|------|------|
| 绿点 | 竹青 #6B8E6B | 4×4px | 有待办（Todo） |
| 红点 | 朱砂 #C03C2F | 6×6px | 有Deadline |

标记规则与v2.0一致：红点大于绿点，横向排列绿左红右，今日为白色。

**DDL名称显示**：
- 日期数字正下方（节假日名称之下）
- 朱砂红色，9px，居中
- 超长省略（text-overflow: ellipsis）
- 多个DDL只显示第一个

**数据联动**：
- 绿点来源于Todo List，红点+名称来源于DDL List
- 主页栏与待办栏数据双向同步

#### 5.2.3 日期详情卡片

**位置**：日历网格下方，宣纸质感卡片。

**标题**：选中日期，如"八月廿三 周日"（农历可选展示）。

**内容**：
- 待办事项区：竹青点 + "待办 (N)"，括号式勾选
- Deadline区：朱砂点 + "DDL (N)"，名称 + 截止标签
- 空状态显示引导文案

---

### 5.3 自习栏（Tab 3）

自习栏从上到下：国风主题切换、番茄钟组件（含专注记录）、线上自习室（含BGM+内置番茄钟）。

#### 5.3.1 国风主题切换

**4种国风主题**：

| 主题名称 | 主色调 | 背景色 | 意境 |
|----------|--------|--------|------|
| 黛青默认 | #2D5F5D | #F5F0E8 | 宣纸青墨，日常使用 |
| 墨夜 | #4A4A6E | #1E1E2E | 夜间护眼，墨色书房 |
| 竹林 | #6B8E6B | #F0F5ED | 竹影清幽，阅读专注 |
| 暮霞 | #B5495B | #FBF0EC | 晚霞暖意，傍晚自习 |

切换方式：顶部4个圆形色块，选中态有黛青描边。

---

#### 5.3.2 番茄钟组件（含专注时长记录）

**位置**：主题切换下方，大卡片。

**整体结构**：
1. 标题栏："番茄钟"（宋体）+ 右上角"+"号
2. 番茄钟卡片滑窗（6格/屏，横向滚动）
3. 大计时器显示
4. 底部控制按钮

**番茄钟卡片滑窗**：
- 一屏6格，大小一致，正方形
- 卡片左侧竖色条区分（同课程色条体系）
- 卡片内容：名称（上）+ 时长分钟数（下）
- 支持自定义背景图（半透明遮罩 + 白色文字）
- scroll-snap 吸附滑动
- 选中卡片有黛青边框

**创建番茄钟**：
- 点击"+"弹出表单
- 字段：名称（1-10字符）、时长（20-180分钟整数）、背景图URL（可选）、分类标签（可选）

**番茄钟分类标签**：
- 创建时可选择分类，如"高数""英语""专业课""其他"
- 分类用于专注时长统计的按类别汇总
- 默认分类为"未分类"

**专注时长记录**（核心新增）：

每次番茄钟计时完成（非手动暂停中断），系统自动生成一条专注记录：

| 字段 | 说明 |
|------|------|
| 记录ID | 唯一标识 |
| 番茄钟名称 | 如"高数复习" |
| 分类标签 | 如"高数" |
| 实际专注时长 | 分钟数（正常完成=设定时长，手动结束=实际已专注时长） |
| 日期 | YYYY-MM-DD |
| 开始时间 | HH:MM |
| 结束时间 | HH:MM |
| 场景 | "独立" 或 "自习室"（标记是否在自习室内完成） |
| 自习室ID | 若在自习室内完成，记录房间ID |

**记录展示**：
- 番茄钟组件标题栏右侧有"记录"入口（小图标）
- 点击进入专注记录列表页
- 列表按日期倒序展示，每条显示：日期、番茄钟名称、时长、分类、场景
- 顶部汇总卡：今日总专注时长、本周总专注时长

**大计时器显示**：
- 超大字号倒计时（分:秒）
- 下方显示番茄钟名称 + 分类标签
- 圆形进度环（黛青色），随时间推进填满

**控制按钮**：
- 开始/暂停（黛青主色圆形）
- 重置（次要样式）
- 计时完成：提示音 + "专注完成" + 自动记录专注数据

**字段规则**：

| 字段 | 类型 | 必填 | 约束 | 默认 |
|------|------|------|------|------|
| 名称 | 文本 | 是 | 1-10字符 | 无 |
| 时长 | 数字 | 是 | 20-180分钟 | 25 |
| 背景图 | URL | 否 | 有效图片URL | 无 |
| 分类标签 | 选择/自建 | 否 | 预设+自定义 | 未分类 |

---

#### 5.3.3 线上自习室（含BGM + 内置番茄钟）

**位置**：番茄钟组件下方，独立大卡片。

**标题栏**："线上自习室" + "ONLINE"标签 + 右侧"+"号。

**Tab切换**："我的" / "全部"。

**点击"+"号**：弹出"创建自习室" / "加入自习室"选择面板。

**创建自习室**：房间名称（必填）+ 背景图URL（可选）。室主可更换背景图。

**加入自习室**：输入房间号加入。

**自习室列表**：
- "我的"Tab：按加入时长降序
- "全部"Tab：按在线人数降序，可滑动浏览
- 室主房间有👑标识
- 点击进入详情页

---

#### 5.3.4 自习室详情页（聊天室 + BGM + 番茄钟）

**页面形态**：全屏覆盖，左上角返回。

**顶部导航栏**：
- 返回按钮 + 房间名（室主带👑）+ 在线人数 + 设置按钮（室主可见）

**自习人员区**：
- 横向滚动成员头像列表
- 每个成员：头像 + 昵称 + 用户ID
- 在线成员绿色圆点，室主皇冠角标

**BGM音乐接入**（核心新增）：

**位置**：聊天区上方，独立横条。

**功能**：
- 显示当前播放状态：歌曲名 + 音乐来源图标 + 播放/暂停按钮
- 默认收起状态，显示"BGM: 未播放 🎵 点击选择"
- 点击展开BGM选择面板

**BGM选择面板**（底部弹出）：
- 三个外部音乐平台入口：
  - QQ音乐（绿色图标）
  - 网云音乐（红色图标）
  - Apple Music（红粉图标）
- 点击任一平台 → 跳转该平台App/网页 → 用户选择歌单/歌曲 → 返回自习室
- 返回后显示：平台图标 + 歌曲名 + 播放控制（播放/暂停/下一首）
- 音乐由外部平台播放，本App仅显示状态和控制按钮（通过系统媒体控制器或平台SDK）
- 室主可选"设为房间BGM"，所有成员同步收听（需各成员也安装对应平台）
- 普通成员仅控制自己的BGM，不影响他人

**技术说明**：
- MVP阶段：通过URL Scheme跳转外部音乐App，本App记录播放状态
- 正式版：通过各平台SDK实现应用内播放控制

**内置番茄钟**（核心新增）：

进入自习室后，页面内保留番茄钟计时功能：
- BGM栏下方有精简版番茄钟控制条
- 显示：当前番茄钟名称 + 倒计时 + 开始/暂停按钮
- 点击控制条可展开番茄钟选择滑窗（同自习栏的6格滑窗）
- 可选择已有番茄钟或快速创建
- 自习室内完成的番茄钟，专注记录中"场景"字段标记为"自习室"，并关联自习室ID
- 退出自习室时若番茄钟正在计时，弹出提示"番茄钟正在计时，退出后将继续在后台计时"

**聊天消息区**：
- 占据中间主要空间
- 消息结构：头像 + 气泡 + 时间
- 自己消息靠右（黛青气泡白字），他人靠左（卡片白气泡墨黑字）

**延迟接收消息机制**：
- 聊天区默认显示历史消息，不实时接收新消息
- 底部黄色横幅："点击展开聊天窗口 · 新消息仅在展开后接收"
- 点击横幅后开始接收新消息
- 离开后恢复未展开状态

**底部输入区**：
- 输入框 + 发送按钮（黛青圆形）
- 输入框默认收起，主动点击才弹出

---

### 5.4 小组件自定义系统

**核心理念**：将各个功能区域拆分为独立小组件，用户可自由添加、移动、删除、装饰。

#### 5.4.1 小组件清单

| 组件名称 | 所属页面 | 默认状态 | 说明 |
|----------|----------|----------|------|
| 印章头部 | 主页 | 固定 | 不可移除，不可移动 |
| 星星按钮 | 主页 | 固定 | 不可移除 |
| 名言警句 | 主页 | 可移动 | 可上下调整位置 |
| 便签 | 主页 | 可移动 | 可上下调整位置 |
| 周课表 | 主页 | 固定 | 不可移除 |
| Todo List | 主页 | 可移动 | 可上下调整位置 |
| DDL List | 主页 | 可移动 | 可上下调整位置 |
| 番茄钟组件 | 自习 | 固定 | 不可移除 |
| 主题切换 | 自习 | 可移动 | 可上下调整 |
| 线上自习室 | 自习 | 固定 | 不可移除 |
| 装饰组件-水墨竹 | 全局 | 可添加 | 纯装饰，无功能 |
| 装饰组件-印章贴 | 全局 | 可添加 | 纯装饰，用户自定义文字 |
| 装饰组件-落款 | 全局 | 可添加 | 纯装饰，显示用户自定义落款 |
| 装饰组件-节气卡 | 全局 | 可添加 | 显示当前节气信息 |

#### 5.4.2 编辑模式

**进入方式**：
- 长按任意可移动小组件 0.5秒
- 或在"我的"页点击"编辑桌面布局"

**编辑态表现**：
- 所有小组件轻微抖动（类似iOS编辑图标）
- 每个可移动组件右上角出现拖拽手柄（≡ 图标）
- 可删除组件左上角出现 × 按钮
- 底部出现"添加组件"按钮栏

**操作**：
- **移动**：长按拖拽手柄，组件跟随手指移动，松开后吸附到最近位置（其他组件自动让位）
- **添加**：点击底部"添加组件" → 弹出组件选择面板 → 选择装饰性组件 → 添加到页面末尾
- **删除**：点击组件左上角 × → 二次确认 → 删除（默认组件删除后可通过"添加组件"恢复）
- **退出**：点击空白处或"完成"按钮退出编辑模式

#### 5.4.3 装饰组件详情

**水墨竹**：
- 纯装饰图片，水墨竹子图案，半透明叠加在背景上
- 可拖动到任意位置，不影响功能组件

**印章贴**：
- 朱砂红方形印章样式，用户可输入1-4个字
- 模拟传统印章效果，可放置在页面任意位置

**落款**：
- 竖排小字，显示用户自定义文字（如"甲辰年 子衿书"）
- 模拟书画落款，淡墨色

**节气卡**：
- 小卡片显示当前节气名称和简短描述
- 如"白露·露凝而白，秋意渐浓"
- 24节气数据内置，自动更新

---

### 5.5 我的页

从主页头部右侧头像入口进入。

**用户信息卡片**：
- 头像、昵称、用户ID
- 点击编辑个人信息

**名言便签库**：
- 查看已收集的名言警句
- 按分类分组展示
- 添加/编辑/删除
- 联动名言警句"个人名言"模式

**学习记录与报告**：
- 专注记录列表（按日期/分类筛选）
- StudyHub月报入口
- 学期总结入口
- 详细说明见第6章

**提醒设置**：
- 提前一周提醒（开关，默认开）
- 提前一天提醒（开关，默认开）
- 提醒方式（手机推送 / 应用内提示）

**课表管理**：
- 拍照导入课表（OCR + LLM解析）
- 自定义名言设置
- 清空课表（二次确认）
- 学期起始日期设置（影响干支纪年周次计算）

**其他设置**：
- 数据备份与恢复
- 编辑桌面布局（进入小组件编辑模式）
- 关于
- 意见反馈

---

## 6. 学习记录与报告系统

### 6.1 数据记录范围

系统自动记录以下数据，无需用户手动操作：

| 数据类型 | 记录内容 | 来源 |
|----------|----------|------|
| Todo完成记录 | 每次标记完成/取消完成的时间、内容 | 主页/月历的Todo操作 |
| DDL完成记录 | DDL是否在截止前完成、完成时间 | DDL操作 |
| 番茄钟专注记录 | 每次专注的时长、分类、日期、场景（独立/自习室） | 番茄钟计时结束 |
| 自习室时长记录 | 每次进入自习室的时长、房间名 | 进入/退出自习室 |
| 深夜学习标记 | 22:00-02:00期间的专注时段 | 番茄钟记录的时间分析 |

### 6.2 StudyHub月报

**生成频率**：每月1日自动生成上月报告。用户也可在"我的"页手动查看历史月报。

**月报内容**：

**概览卡**：
- 月度专注总时长（小时）
- 月度自习室总时长（小时）
- Todo完成数 / 总数
- DDL完成数 / 总数
- 深夜学习天数

**专注时长分析**：
- 按分类的专注时长分布（柱状图，如高数10h/英语8h/专业课5h）
- 按日期的专注趋势（折线图，每日专注时长）
- 最专注的一天（日期 + 时长）

**自习室分析**：
- 本月自习总时长
- 最常去的自习室（房间名 + 时长）
- 自习室专注时长占比

**任务完成情况**：
- Todo完成率（完成数/创建数）
- DDL完成率 + 按时完成率
- 未完成的DDL列表（提醒）

**鼓励寄语**：
- 根据数据自动生成一句鼓励语
- 示例："这个月你专注了120小时，比上个月多了15小时。坚持的力量，日久可见。"
- 若数据较上月下降，则生成温和鼓励："这个月可能有些忙，专注时长有所减少，下个月继续加油！"

### 6.3 学期末交互式总结

**触发时机**：学期结束日期前一周，App启动时弹出提示"本学期即将结束，来看看你的学期总结吧！"。用户可立即查看或稍后查看。

**页面形态**：全屏沉浸式页面，宣纸米白背景，宋体大字，逐句展示。

**交互方式**：
- 每次屏幕只显示一句话
- 用户点击屏幕任意位置 → 当前句淡出，下一句淡入
- 最后一句显示完后，底部出现"查看完整报告"按钮

**总结内容序列**（根据实际数据动态生成）：

1. "你过去的一学期，有{N}天学习到深夜。"（深夜学习天数）
2. "完成了{N}个Todo，{M}个DDL。"（任务完成数）
3. "番茄钟总专注{N}小时，{分类}占比最高。"（专注总时长+最高分类）
4. "在自习室度过了{N}小时，最常去的是「{房间名}」。"（自习室时长）
5. "你最专注的一天是{日期}，那天专注了{N}分钟。"（最高单日记录）
6. "你定的学期目标是：{规划内容}。"（读取星星按钮的学期规划）
7. 根据目标完成情况：
   - 达成："恭喜你，目标实现了。" + 朱砂印章"达成"盖印动画
   - 部分达成："虽然有些目标还没完全实现，但你已经走了很远。"
   - 未达成："目标还在前方，下个学期继续追赶。"

**完整报告页**：
- 点击"查看完整报告"进入详细数据页
- 包含月报级别的图表和详细分类数据
- 可分享为图片（国风风格总结卡，带印章和水墨元素）

---

## 7. 数据模型

### 7.1 核心数据实体

**课程（Course）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 课程名称 |
| location | string | 上课地点 |
| weekday | number | 星期几（0-6） |
| startSection | number | 起始节次 |
| duration | number | 持续节数 |
| colorIndex | number | 竖色条索引（0-7） |
| source | string | 来源："imported"（导入）或 "manual"（手动添加） |

**节次时间定义（SectionTime）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| sectionIndex | number | 节次序号 |
| name | string | 节次名称（如"第一节"、"晚课"） |
| startTime | string | 开始时间 HH:MM |
| endTime | string | 结束时间 HH:MM |
| isCustom | boolean | 是否用户自主添加 |

**待办（Todo）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| content | string | 待办内容 |
| date | string | 日期 MM/DD |
| completed | boolean | 是否完成 |
| completedAt | timestamp | 完成时间（用于记录统计） |
| createdAt | timestamp | 创建时间 |

**截止日期（Deadline）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | DDL名称 |
| dueDate | string | 截止日期 MM/DD |
| completed | boolean | 是否完成 |
| completedAt | timestamp | 完成时间 |
| createdAt | timestamp | 创建时间 |

**便签（StickyNote）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| content | string | 便签内容（自由书写） |
| updatedAt | timestamp | 最后更新时间 |

**番茄钟配置（PomodoroConfig）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 番茄钟名称 |
| duration | number | 时长（分钟，20-180） |
| bgImage | string | 背景图URL |
| category | string | 分类标签 |
| createdAt | timestamp | 创建时间 |

**专注记录（FocusRecord）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| pomodoroName | string | 番茄钟名称 |
| category | string | 分类标签 |
| actualDuration | number | 实际专注时长（分钟） |
| date | string | 日期 YYYY-MM-DD |
| startTime | string | 开始时间 HH:MM |
| endTime | string | 结束时间 HH:MM |
| scene | string | 场景："independent" / "study_room" |
| studyRoomId | string | 自习室ID（若在自习室中完成） |
| isLateNight | boolean | 是否深夜学习（22:00-02:00） |

**自习室时长记录（StudyRoomRecord）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| studyRoomId | string | 自习室ID |
| studyRoomName | string | 自习室名称 |
| date | string | 日期 YYYY-MM-DD |
| duration | number | 时长（分钟） |
| enterTime | string | 进入时间 |
| exitTime | string | 退出时间 |

**名言警句配置（QuoteConfig）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| mode | string | 模式："fixed" / "random_system" / "random_personal" |
| fixedContent | string | 固定模式下的名言内容 |
| fixedAuthor | string | 固定模式下的作者 |
| lastRandomDate | string | 上次随机刷新日期 |
| lastRandomContent | string | 上次随机内容 |

**名言便签库（QuoteNote）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| content | string | 名言内容 |
| source | string | 来源（书名/电影名/人名） |
| category | string | 分类标签 |
| createdAt | timestamp | 创建时间 |

**学期规划（SemesterPlan）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| content | string | 规划内容 |
| semesterStart | string | 学期开始日期 |
| semesterEnd | string | 学期结束日期 |
| updatedAt | timestamp | 更新时间 |

**自习室（StudyRoom）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 房间号 |
| name | string | 房间名称 |
| ownerId | string | 室主ID |
| bgImage | string | 背景图URL |
| createdAt | timestamp | 创建时间 |
| onlineCount | number | 在线人数 |

**BGM配置（BGMConfig）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| platform | string | 平台："qqmusic" / "netease" / "applemusic" |
| songName | string | 歌曲名 |
| playlistId | string | 歌单ID（可选） |
| isPlaying | boolean | 播放状态 |
| isRoomBGM | boolean | 是否设为房间BGM（室主） |

**小组件布局（WidgetLayout）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| page | string | 所属页面："home" / "study" |
| widgetType | string | 组件类型 |
| position | number | 排列顺序 |
| config | json | 组件配置（如印章贴文字、落款内容等） |
| isVisible | boolean | 是否显示 |

**用户（User）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 用户ID |
| nickname | string | 昵称 |
| avatar | string | 头像URL |
| theme | string | 当前主题 |
| quoteConfigId | string | 名言配置ID |
| semesterPlanId | string | 学期规划ID |

### 7.2 数据关系

```
用户 1──N 课程
用户 1──N 节次时间定义
用户 1──N 待办
用户 1──N 截止日期
用户 1──1 便签
用户 1──N 番茄钟配置
用户 1──N 专注记录
用户 1──N 自习室时长记录
用户 1──1 名言警句配置
用户 1──N 名言便签库
用户 1──1 学期规划
用户 1──N BGM配置
用户 1──N 小组件布局
用户 1──N 自习室（成员）
自习室 1──N 专注记录（场景=自习室）
自习室 1──N 自习室时长记录
番茄钟配置 1──N 专注记录
```

---

## 8. 非功能需求

### 8.1 性能
- 页面切换帧率 ≥ 60fps
- 课表渲染 ≤ 300ms
- 番茄钟计时误差 ≤ 1s/小时
- 主题切换过渡 0.3s
- 小组件拖拽跟手延迟 ≤ 16ms

### 8.2 可用性
- 核心操作（添加Todo/DDL、创建番茄钟、写便签）≤ 2步
- 主要按钮 ≥ 44×44px
- 正文 ≥ 14px，辅助信息 ≥ 10px
- 便签手写体字号 ≥ 16px（保证可读）

### 8.3 兼容性
- 适配 iPhone SE（375px）到 iPhone 14 Pro Max（430px）
- iOS 14+ / Android 10+
- 竖屏（MVP阶段）

### 8.4 数据持久化
- 全部数据本地存储（MVP阶段）
- 重启不丢失
- 后续版本支持云同步

### 8.5 国风视觉一致性
- 所有页面统一使用国风色彩体系
- 字体加载：思源宋体 + 思源黑体（开源字体，无版权风险）
- 印章图标为原创设计或开源素材
- 背景纹理为CSS生成（宣纸纹理用CSS noise/grain效果模拟），不依赖外部图片

---

## 9. MVP范围与优先级

### 9.1 P0 — 必须实现

| 模块 | 功能点 |
|------|--------|
| 全局 | 国风视觉系统（宣纸米白/黛青/朱砂/宋体黑体/细线卡片） |
| 全局 | 印章图标 + 干支纪年头部 |
| 全局 | 底部三Tab（主页/待办/自习） |
| 主页 | 星星按钮 + 学期规划 |
| 主页 | 名言警句三种模式（固定/随机系统/随机个人） |
| 主页 | 便签功能（自由书写） |
| 主页 | 周视图课表（含法定节假日标注） |
| 主页 | 课表时间栏可改动 + 自主添加课时 |
| 主页 | 课程卡片竖色条 |
| 主页 | 极简Todo List（括号式） |
| 主页 | 极简DDL List（月/日 + 朱批红条） |
| 待办 | 月历网格 + 双点标记 + DDL名称 |
| 待办 | 日期详情 |
| 待办 | 主页与月历双向同步 |
| 我的 | 名言便签库 |

### 9.2 P1 — 重要但可稍缓

| 模块 | 功能点 |
|------|--------|
| 自习 | 国风主题切换（4种） |
| 自习 | 自定义番茄钟（6格滑窗 + 创建/命名/时长/背景图/分类） |
| 自习 | 番茄钟计时 + 专注时长记录 |
| 自习 | 专注记录列表页 |
| 自习 | 线上自习室（创建/加入/列表） |
| 自习 | 自习室详情页（人员+聊天+延迟接收） |
| 自习 | 自习室内置番茄钟（持续计时+场景标记） |
| 自习 | BGM接入（QQ音乐/网易云音乐/Apple Music） |
| 自习 | 室主更换背景图 |
| 全局 | 小组件自定义系统（移动/添加/删除） |
| 全局 | 装饰组件（水墨竹/印章贴/落款/节气卡） |

### 9.3 P2 — 后续迭代

| 功能点 | 说明 |
|--------|------|
| StudyHub月报 | 每月自动生成学习报告 |
| 学期末交互式总结 | 沉浸式逐句展示+完整报告 |
| 拍照导入课表 | OCR + LLM结构化解析 |
| 数据云同步 | 多设备同步 |
| DDL推送提醒 | 系统推送通知 |
| BGM应用内播放 | 通过各平台SDK实现应用内控制 |
| 月报/总结分享 | 生成国风风格图片分享 |
| 自习室排行榜 | 学习时长排名 |

---

## 10. 交互亮点总结

1. **国风视觉体系**：宣纸米白底 + 黛青朱砂 + 宋体黑体 + 印章干支纪年，淡雅书卷气与现代功能并存
2. **课程竖色条**：左侧细色条一眼区分不同课程，国风色彩不刺眼
3. **朱批红条紧急DDL**：距截止3天的DDL出现朱砂红条，模拟古代朱批批注
4. **便签自由书写**：模拟实物便签纸，随手记灵感不受格式束缚
5. **名言三模式 + 便签库**：固定/随机系统/随机个人三种模式，名言便签库收集读书观影所得
6. **星星按钮学期规划**：一开学就定目标，学期末对照总结，让努力有方向
7. **法定节假日标注**：课表直接显示"清明""中秋"等节日，假期安排一目了然
8. **可改动时间栏 + 自主加课时**：适配各校作息，自主课时与导入数据不冲突
9. **番茄钟专注记录**：每次专注按分类/日期/场景记录，数据可追溯
10. **自习室内置番茄钟**：进自习室不停计时，场景标记区分独立/自习室专注
11. **BGM外部接入**：QQ音乐/网易云/Apple Music自由切换，用自己的歌单
12. **小组件自定义**：功能组件可移动，装饰组件可添加，打造专属国风桌面
13. **StudyHub月报**：每月自动总结专注/自习/任务数据，给予鼓励
14. **学期末交互总结**：逐句点击展示，"你有50天学习到深夜"，努力被看见

---

> StudyHub 课管助手 PRD v3.0 · 2026-08-23
>
> 配套交互原型：prototype.html
