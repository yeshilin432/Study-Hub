# Study-Hub PRD v2.0

> **产品需求文档 · 第二版**
>
> 面向大学生的课程管理与学习陪伴工具

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-22 | 初稿：课表、月历、Todo、自习室、设置五大模块 |
| v2.0 | 2026-08-23 | 重大迭代：周视图课表、名言警句、极简Todo/DDL双列、月历双点标记、自定义番茄钟、线上自习室、聊天室。全新重构版：补充用户画像、数据模型、交互细节，结构完整化 |

---

## 1. 需求背景

### 1.1 业务触发源

大学生群体每学期都面临课表管理、作业Deadline追踪、自习安排等多重需求。目前市场上的解决方案存在明显断层：超级课程表等工具功能臃肿、广告泛滥；纯Todo工具缺少课表场景；番茄钟App缺乏社交陪伴感，无法满足线上“学习”搭子的要求。学生需要一个轻量、集成、无干扰的工具，将课表、待办、Deadline、番茄钟、线上自习等场景串联起来。

### 1.2 用户痛点

**线上社交：**
- 市面上已有的番茄钟使用场景有限，不方便，满足的核心需求是围绕“用户个人”展开的，而没有满足“学习搭子”或“学习社群”的潜在需求，无法提供合适的线上自习空间。

**多功能集成：**

- 教务系统课表查看场景受限，大部分情况学生使用查看截图的方式，抑或是在网页查看，此时无法离线随时查看。对于市面上已有的课程表app，例如：wakeup课程表，使用场景十分有限，除了导入课表以外便是课程广告，没有其他集成功能，实用性极低。
-
- 课表变动后手动修改流程繁琐
- 无论是课程表app还是番茄钟app，个性化服务的程度还不够，例如：自定义小组件，难以满足用户的情绪价值。
- 各家产品功能有限，缺少集成，用户只能在多个软件之间流转，“手动集成”各大功能。能完成高度自定义需求的如飞书、obsidian与notion软件，上手难度高，功能不简约，使用门槛劝退大多数大学生。
- 作业Deadline散落在各课程群通知中，容易遗漏。无法直观看到一周/一月内有哪些截止任务。
- 没有线上todolist。

### 1.3 需求目标

- **一屏效率**：首页看完课表 → 看到待办 → 看到DDL，全程无需切换页面
- **视觉分层**：通过颜色、大小、位置建立清晰的信息优先级
- **极简干扰**：聊天消息不主动推送，专注时不受打扰
- **陪伴感**：线上自习室提供"有人一起学"的氛围，但不强制互动

---

## 2. 用户画像与核心场景

### 2.1 目标用户

| 用户类型 | 特征 | 核心需求 | 使用频率 |
|----------|------|----------|----------|
| 学霸型 | 985/211，目标明确，每天固定自习 | 番茄钟定制、学习记录、高效管理 | 每日 |
| 普通型 | 上课+作业+偶尔突击，学习节奏随Deadline走 | 课表查看、DDL提醒、月历规划 | 每周3-5次 |
| 摸鱼型 | 拖延症严重，Deadline前才开始动 | DDL预警、线上自习监督、陪伴感 | 每周1-2次 |
| 社团/班委 | 事务多，日程杂，容易忘事 | Todo清单、月历标记、分类管理 | 每日 |

### 2.2 核心使用场景

**场景一：早上起床看今日安排**
> 小明7:50起床，打开课管助手，首页顶部看到"日拱一卒，功不唐捐"的名言，往下扫一眼周课表——上午一二节有高数，再往下看Todo：今天要交英语作业。DDL那边显示后天是数据结构大作业截止。心里有数了，起床去上课。

**场景二：课间随手记作业**
> 高数课上老师布置了课后习题，小明点开课表页左下角的Todo"+"号，输入"高数习题P32"，选了今天的日期，完成。月历上今天的格子里自动多了个绿点。

**场景三：晚上自习用番茄钟**
> 晚上9点，小明打开自习页，选了"森林"主题，在番茄钟滑窗里找到自己创建的"英语听力 40min"卡片，点击开始。番茄钟倒计时开始，小明戴上耳机开始听听力。

**场景四：和室友线上一起自习**
> 周末想学习但宿舍太吵，小明创建了一个叫"402学习小组"的线上自习室，把房间号发给室友们。大家陆续加入，头像排在上边，聊天框收起来不打扰。安安静静学了一下午，想说话的时候点一下聊天框展开就行。

---

## 3. 产品架构

### 3.1 信息架构

```
课管助手
├── 课表页（首页）
│   ├── 名言警句（可自定义）
│   ├── 周视图课表（7天并列）
│   └── 底部双列
│       ├── Todo List（括号式勾选）
│       └── DDL List（月/日格式）
├── 月历页
│   ├── 月份导航
│   ├── 日历网格（双点标记 + DDL名称）
│   └── 日期详情（Todo/DDL分区展示）
├── 自习页
│   ├── 主题切换（4种主题）
│   ├── 番茄钟组件（自定义 + 6格滑窗）
│   └── 线上自习室（创建/加入/聊天室）
└── 我的页
    ├── 个人信息
    ├── 提醒设置
    ├── 课表管理
    └── 其他设置
```

### 3.2 核心设计原则

1. **一屏原则**：相关信息尽量放在同一屏，减少切换
2. **视觉分层**：用颜色和大小区分优先级（红点 > 绿点，DDL > Todo）
3. **极简交互**：点击即完成，减少弹窗和步骤
4. **不打扰原则**：消息不主动推送，用户主动展开才接收
5. **可定制性**：名言、主题、番茄钟、背景图都让用户自己选

---

## 4. 功能需求详情

### 4.1 课表页

课表页是App启动后的默认页面，从上到下分为三个区域：名言警句横幅、周视图课表、底部极简双列表。

#### 4.1.1 名言警句

**页面位置**：课表页最顶部，全宽横幅。

**布局结构**：
- 渐变背景，文字居中
- 主文字为名言内容（斜体）
- 右下角为作者/出处（更小字号，浅灰色）
- 右上角有一个大号半透明引号装饰

**业务逻辑**：
- 首次使用默认展示预设名言："星光不问赶路人，时光不负有心人。—— 佚名"
- 点击横幅任意位置，弹出编辑弹窗
- 编辑弹窗包含两个输入框：名言内容、作者/出处
- 保存后立即更新顶部显示，并持久化存储
- 重启App后保留用户自定义的名言

**交互细节**：
- 点击横幅时有轻微上浮动画（translateY -2px），提供点击反馈
- 编辑弹窗从底部滑入，背景半透明遮罩
- 输入框为空时保存按钮置灰，不可点击

**字段规则**：

| 字段 | 类型 | 必填 | 长度限制 | 默认值 |
|------|------|------|----------|--------|
| 名言内容 | 文本 | 是 | 1-50字符 | 星光不问赶路人，时光不负有心人。 |
| 作者/出处 | 文本 | 否 | 1-20字符 | 佚名 |

---

#### 4.1.2 周视图课表

**页面位置**：名言横幅下方，占据页面中部核心区域。

**布局结构**：
- 顶部为星期标题行：左侧空白（时间列占位），右侧7列分别为周一至周日
- 每列标题分两行：上行"周X"，下行日期数字（如"23"）
- 今日对应的列：日期数字用主色高亮显示
- 左侧为时间列：显示节次编号和开始时间（如"1 / 08:00"）
- 右侧7列×N行的课程网格：有课时显示彩色课程卡片，无课时为空白格

**课程卡片**：
- 卡片颜色：8种预设颜色循环分配，不同课程用不同颜色区分
- 卡片内容：课程名称（加粗）+ 上课地点（更小字号，两行布局）
- 跨节课程：卡片高度随跨节数变化，占满对应节次的格子
- 点击课程卡片：弹出课程详情/编辑表单

**编辑模式**：
- 右上角"编辑"按钮，点击进入编辑模式
- 编辑模式下，空白格子中央显示"+"号（浅灰色）
- 点击空白格子：弹出"添加课程"表单
- 点击已有课程：弹出"编辑课程"表单（底部含删除按钮）
- 再次点击"完成"退出编辑模式

**课程表单字段**：

| 字段 | 类型 | 必填 | 约束 | 默认 |
|------|------|------|------|------|
| 课程名称 | 文本 | 是 | 1-20字符，建议用简称 | 无 |
| 上课地点 | 文本 | 否 | 1-30字符，如"A-301" | 无 |
| 上课星期 | 单选 | 是 | 周一至周日 | 选中格子对应的星期 |
| 起始节次 | 单选 | 是 | 第1-12节 | 选中格子对应的节次 |
| 持续节数 | 数字 | 是 | 1-4节 | 1 |

**空状态**：
- 课表完全为空时，显示引导文案和"添加课程"按钮
- 提供"拍照导入课表"入口（MVP阶段为占位按钮）

---

#### 4.1.3 底部极简双列表

**页面位置**：周课表下方，左右两列并排，各占50%宽度。

**左侧：待办列表（TodoList）**

标题栏：左侧绿点 + "待办"文字 + 右侧"+"添加按钮。

列表项规则：
- 每行结构：左侧空心括号 `[ ]` + 待办文字
- 点击括号 → 括号变为 ✅ + 文字变灰加删除线 → 标记完成
- 再次点击已完成项的 ✅ → 恢复未完成状态
- 待办按创建时间倒序排列（最新的在上面）
- 已完成项保留在列表中，但视觉上弱化（灰色 + 删除线）
- 最多显示5条，超出部分在原型中省略（正式版可滚动）

添加Todo：
- 点击"+"按钮弹出添加表单
- 表单字段：待办内容（必填）、日期（可选，默认今天）
- 日期格式为选择器，选择后格式化为"月/日"
- 添加后：① 出现在Todo列表顶部 ② 对应日期的月历格子出现绿点

**右侧：DDL列表**

标题栏：左侧红点 + "DDL"文字 + 右侧"+"添加按钮。

列表项规则：
- 每行结构：DDL名称（左对齐）+ 右侧日期标签（如"09/03"）
- 按截止日期升序排列（最近的在上面）
- 日期标签背景为浅红色，文字为红色
- 已过期的DDL：日期标签变灰色，文字加删除线
- 最多显示5条，超出部分省略

添加DDL：
- 点击"+"按钮弹出添加表单
- 表单字段：DDL名称（必填）、截止日期（必填）
- 日期为选择器，格式统一为 `月/日`（如 `09/3` 表示九月三日）
- 添加后：① 出现在DDL列表 ② 对应日期的月历格子出现红点 + 显示DDL名称

**双列表字段规则**：

| 类型 | 字段 | 类型 | 必填 | 格式 |
|------|------|------|------|------|
| Todo | 内容 | 文本 | 是 | 1-30字符 |
| Todo | 日期 | 日期 | 否 | 月/日，默认今天 |
| DDL | 名称 | 文本 | 是 | 1-20字符 |
| DDL | 截止日期 | 日期 | 是 | 月/日 |

---

### 4.2 月历页

月历页是第二个Tab，从上到下分为：月份导航栏、日历网格、日期详情卡片。

#### 4.2.1 月份导航

- 左侧显示当前年月，如"2026年8月"
- 右侧三个按钮：左箭头（上个月）、"今天"按钮（回到当月并选中今日）、右箭头（下个月）
- 左右切换有平滑滑动动画

#### 4.2.2 日历网格

**格子结构**（每个日期格子内从上到下）：
1. 日期数字（居中）
2. DDL名称（红色小字，过长省略）
3. 标记点区域（绿点+红点，横向排列）

**日期状态样式**：

| 状态 | 样式 |
|------|------|
| 普通日期 | 黑色数字，白色背景 |
| 今日 | 主色圆形填充背景，白色数字 |
| 选中日期 | 淡主色背景 + 主色描边圆形 |
| 非本月日期 | 浅灰色数字 |

**DDL名称显示**：
- 位置：日期数字正下方
- 样式：红色，字号约9px，居中对齐
- 内容：该日期第一个DDL的名称
- 超长处理：`text-overflow: ellipsis`，超出1行省略
- 今日的DDL名称：白色文字（因为今日有主色填充背景）
- 多个DDL时只显示第一个，其余通过标记点体现

**双点标记系统**：

| 标记 | 颜色 | 尺寸 | 含义 |
|------|------|------|------|
| 绿点 | #22C55E | 4×4px | 该日期有待办（Todo） |
| 红点 | #FF6B6B | 6×6px | 该日期有Deadline |

标记规则：
- 红点比绿点大（视觉上强调DDL的紧急程度更高）
- 两个点横向排列，绿左红右，居中对齐
- 同一类型有多个时只显示一个点，不叠加
- 今日格子中的点为白色
- 没有对应类型的任务时，该位置不显示点

**数据联动**：
- 绿点数据完全来源于Todo List
- 红点 + DDL名称数据完全来源于DDL List
- 在课表页添加/删除/完成Todo或DDL后，切换到月历页自动更新
- 双向同步：在月历页的日期详情中操作Todo/DDL，课表页也同步更新

#### 4.2.3 日期详情卡片

**位置**：日历网格下方，白色圆角卡片。

**标题**：显示选中的完整日期，如"8月23日 周日"。

**内容分两部分**：

**待办事项区**：
- 小标题：绿点 + "待办事项 (N)"
- 每条待办：左侧空心括号 `[ ]` + 待办文字
- 点击括号标记完成（变✅+删除线），交互与课表页Todo一致
- 空状态：显示"暂无待办"（浅灰色，居中）

**Deadline区**：
- 小标题：红点 + "Deadline (N)"
- 每条DDL：名称（左侧）+ "截止"标签（右侧，红色背景）
- 空状态：显示"暂无Deadline"（浅灰色，居中）

**交互**：
- 点击不同日期，下方卡片内容即时切换
- 卡片高度随内容自适应
- 在卡片中操作Todo/DDL（完成/删除），日历网格的标记点同步更新

---

### 4.3 自习页

自习页是第三个Tab，从上到下分为：主题切换、番茄钟组件、线上自习室区块。

#### 4.3.1 主题切换

**位置**：自习页顶部，居中横向排列。

**功能**：
- 4个圆形色块，代表4种主题
- 点击切换全局主题色（自习页范围内）
- 当前选中的主题有外圈描边（白色 + 阴影）
- 切换主题有0.3秒平滑过渡动画

**4种主题定义**：

| 主题名称 | 主色调 | 背景色 | 卡片色 | 适用场景 |
|----------|--------|--------|--------|----------|
| 经典蓝紫 | #4F6DF5 | #F5F6FA | #FFFFFF | 白天日常使用 |
| 暗夜 | #6366F1 | #1A1A2E | #252542 | 夜间护眼 |
| 森林 | #2D8B56 | #F0F7F0 | #FFFFFF | 阅读/深度专注 |
| 日落 | #E8743B | #FFF5F0 | #FFFFFF | 傍晚/温暖氛围 |

---

#### 4.3.2 番茄钟组件

**位置**：主题切换下方，大圆角卡片。

**整体结构**（从上到下）：
1. 标题栏："🍅 我的番茄钟" + 右上角"+"号按钮
2. 番茄钟卡片滑窗（横向滚动，6格/屏）
3. 当前选中番茄钟的大计时器显示
4. 底部控制按钮（开始/暂停、重置）

**番茄钟卡片滑窗**：
- 一屏均匀显示6个番茄钟卡片，大小完全一致，正方形
- 卡片宽度 = (组件总宽 - 左右padding - 5个间距) / 6
- 支持左右滑动查看更多，scroll-snap 吸附（每次滑动一格或一屏）
- 选中的卡片有主色边框（2px），略放大一点（scale 1.05）
- 卡片内容：名称（上）+ 时长分钟数（下，更大字号）
- 支持自定义背景图片：图片填充整个卡片，上面加半透明黑色遮罩（保证文字可读），文字白色显示在遮罩上层

**创建番茄钟**：
- 点击右上角"+"号，弹出创建表单
- 表单字段：
  - 名称（必填，1-10字符，如"英语听力"）
  - 专注时长（必填，20-180分钟，整数，步进器或输入框）
  - 背景图片URL（可选，输入框或选择器）
- 创建后自动添加到滑窗末尾，可向右滚动查看
- 创建后自动选中这个新番茄钟

**大计时器显示**：
- 超大字号显示倒计时（分:秒格式，如"25:00"）
- 下方小字显示当前番茄钟名称
- 圆形进度环围绕倒计时，随时间推进逐渐填满

**控制按钮**：
- 左侧：重置按钮（圆形，次要样式）
- 右侧：开始/暂停按钮（圆形，主色填充，更大）
- 计时结束：播放提示音 + 弹出"专注完成！"提示 + 自动重置

**字段规则**：

| 字段 | 类型 | 必填 | 约束 | 默认 |
|------|------|------|------|------|
| 名称 | 文本 | 是 | 1-10字符 | 无 |
| 时长 | 数字 | 是 | 20-180分钟，整数 | 25 |
| 背景图片 | URL | 否 | 有效图片URL | 无（纯色背景） |

**边界情况**：
- 番茄钟数量 < 6个时，滑窗不可滚动，卡片均匀分布
- 番茄钟数量 = 0时，滑窗区域显示空状态提示 + "点击右上角创建第一个番茄钟"

---

#### 4.3.3 线上自习室区块

**位置**：番茄钟组件下方，独立大卡片。

> 注意：此处的"自习室"指**线上自习室**（虚拟房间），不是线下物理自习室。UI上通过"ONLINE"标签和房间图标明确区分。

**顶部标题栏**：
- 左侧："🏠 线上自习室"文字
- 旁边：绿色"ONLINE"小标签
- 右侧："+"号按钮

**Tab切换**：
- "我的" / "全部" 两个Tab
- 选中的Tab有主色下划线
- 默认选中"我的"

**点击"+"号**：
- 弹出半屏选择面板，两个选项并排：
  - 左侧：「创建自习室」+ ➕图标 + 副标题"自己当室主"
  - 右侧：「加入自习室」+ 🚪图标 + 副标题"输入房间号"
- 点击选项后进入对应表单

**创建自习室**：
- 表单字段：房间名称（必填，1-20字符）、背景图片URL（可选）
- 创建后自动加入，创建者为室主
- 室主特殊权限：可更换房间背景图
- 新房间出现在"我的"Tab列表第一位

**加入自习室**：
- 表单字段：房间号（必填，6位数字或字母）
- 验证通过后加入房间
- 房间出现在"我的"Tab列表

**自习室列表**：
- 每条目结构：左侧房间头像（渐变色圆形）+ 中间房间信息 + 右侧在线人数
- 房间信息：房间名（第一行，加粗）+ 状态（第二行，小字）
- 状态文字："我的"Tab显示"已加入N天"，"全部"Tab显示"X人在线"
- 室主的房间在名称旁有 👑 标识
- "我的"Tab排序规则：按加入时长降序（加入越久越靠前）
- "全部"Tab排序规则：按在线人数降序（热门在前）
- "全部"Tab支持上下滑动浏览更多房间
- 点击房间条目进入自习室详情页

---

#### 4.3.4 自习室详情页（聊天室）

**进入方式**：点击自习室列表中的任意房间。

**页面形态**：全屏覆盖页，左上角有返回按钮。

**顶部导航栏**：
- 返回按钮（‹）
- 房间名称（室主房间带 👑）
- 右侧：在线人数 + 设置按钮（⚙️，仅室主可见）
- 室主点击设置可更换房间背景图

**自习人员区**：
- 横向滚动的成员头像列表
- 每个成员：圆形头像 + 下方昵称 + 更下方用户ID（小字灰色）
- 在线成员：头像右下角有绿色小圆点
- 自己的头像：有主色描边
- 室主头像：有小皇冠角标
- 最多显示一行，超出可左右滑动

**聊天消息区**：
- 占据页面中间主要空间
- 消息结构：头像 + 消息气泡 + 时间
- 自己的消息：靠右显示，主色气泡，白色文字
- 他人的消息：靠左显示，白色/浅色气泡，深色文字
- 系统消息：居中显示，灰色小字

**延迟接收消息机制**（核心设计）：
- 聊天区默认显示历史消息，但不实时接收新消息
- 消息区底部有一条黄色横幅："💬 点击展开聊天窗口 · 新消息仅在展开后接收"
- 用户点击横幅后，横幅文字变为"已展开聊天，正在接收新消息"，同时开始模拟接收新消息
- 目的：保证专注学习时不被聊天打扰，想聊天时再主动展开
- 离开聊天室后重新进入，恢复为"未展开"状态

**底部输入区**：
- 输入框（左，圆角）+ 发送按钮（右，圆形主色）
- 输入框默认收起，需要用户主动点击才弹出键盘
- 发送后消息立即出现在聊天区（自己的消息）

---

### 4.4 我的页

我的页是第四个Tab，从上到下依次为：

**用户信息卡片**：
- 头像、昵称、用户ID
- 点击可编辑个人信息

**提醒设置组**：
- 提前一周提醒（开关，默认开启）
- 提前一天提醒（开关，默认开启）
- 提醒方式（进入二级页面设置：手机推送 / 小程序内提示）

**课表管理组**：
- 拍照导入课表（跳转导入流程）
- 自定义名言（跳转名言编辑）
- 清空课表（危险操作，二次确认）

**其他设置**：
- 数据备份与恢复
- 关于我们
- 意见反馈

---

## 5. 数据模型

### 5.1 核心数据实体

**课程（Course）**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 课程名称 |
| location | string | 上课地点 |
| weekday | number | 星期几（0-6，0=周日） |
| startSection | number | 起始节次（1-12） |
| duration | number | 持续节数（1-4） |
| colorIndex | number | 颜色索引（0-7） |

**待办（Todo）**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| content | string | 待办内容 |
| date | string | 日期，格式"MM/DD" |
| completed | boolean | 是否完成 |
| createdAt | timestamp | 创建时间 |

**截止日期（Deadline）**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | DDL名称 |
| dueDate | string | 截止日期，格式"MM/DD" |
| createdAt | timestamp | 创建时间 |

**番茄钟（Pomodoro）**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 番茄钟名称 |
| duration | number | 时长（分钟，20-180） |
| bgImage | string | 背景图片URL（可选） |
| createdAt | timestamp | 创建时间 |

**自习室（StudyRoom）**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 房间号（唯一） |
| name | string | 房间名称 |
| ownerId | string | 室主用户ID |
| bgImage | string | 背景图片URL（可选） |
| createdAt | timestamp | 创建时间 |
| onlineCount | number | 当前在线人数 |

**用户（User）**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 用户ID |
| nickname | string | 昵称 |
| avatar | string | 头像URL |
| quote | string | 自定义名言 |
| quoteAuthor | string | 名言作者 |
| theme | string | 当前主题 |

### 5.2 数据关系

```
用户 1──N 课程
用户 1──N 待办
用户 1──N 截止日期
用户 1──N 番茄钟
用户 1──N 自习室（作为成员）
自习室 N──1 用户（室主）
自习室 M──N 用户（成员）
```

---

## 6. 非功能需求

### 6.1 性能

- 页面切换动画帧率 ≥ 60fps
- 课表渲染时间 ≤ 300ms（12节×7天网格）
- 番茄钟计时误差 ≤ 1s/小时
- 主题切换过渡时间 = 0.3s

### 6.2 可用性

- 核心操作（添加Todo/DDL、创建番茄钟）不超过2步完成
- 主要按钮尺寸 ≥ 44×44px，符合移动端触控标准
- 文字最小字号 ≥ 10px（辅助信息），正文 ≥ 14px

### 6.3 兼容性

- 适配 iPhone SE（375px宽）到 iPhone 14 Pro Max（430px宽）
- 支持 iOS 14+ / Android 10+
- 横竖屏：仅支持竖屏（MVP阶段）

### 6.4 数据持久化

- 用户数据本地存储（MVP阶段）
- 页面刷新/重启App后数据不丢失
- 后续版本支持云同步

---

## 7. MVP范围与优先级

### 7.1 P0 — 必须实现

| 模块 | 功能点 |
|------|--------|
| 课表页 | 名言警句自定义 |
| 课表页 | 周视图课表展示 |
| 课表页 | 课程手动添加/编辑/删除 |
| 课表页 | 极简Todo List（括号式勾选） |
| 课表页 | 极简DDL List（月/日格式） |
| 月历页 | 日历网格展示 |
| 月历页 | 双点标记（绿点Todo / 红点DDL） |
| 月历页 | 日期格子显示DDL名称（超长省略） |
| 月历页 | 日期详情（Todo/DDL分区展示） |
| 月历页 | 课表页与月历页数据双向同步 |
| 我的页 | 提醒设置（开关 + 方式选择） |

### 7.2 P1 — 重要但可稍缓

| 模块 | 功能点 |
|------|--------|
| 自习页 | 4种主题切换 |
| 自习页 | 自定义番茄钟（创建/命名/时长/背景图） |
| 自习页 | 番茄钟6格滑窗横向滚动 |
| 自习页 | 番茄钟计时功能 |
| 自习页 | 线上自习室列表（我的/全部） |
| 自习页 | 创建自习室 / 加入自习室 |
| 自习页 | 自习室聊天室 |
| 自习页 | 延迟接收消息机制 |
| 自习页 | 室主更换背景图权限 |

### 7.3 P2 — 后续迭代

| 功能点 | 说明 |
|--------|------|
| 拍照导入课表 | OCR + LLM 结构化解析 |
| 数据云同步 | 多设备数据同步 |
| DDL推送提醒 | 微信订阅消息 / 手机推送 |
| 学习数据统计 | 番茄钟周报/月报 |
| 自习室排行榜 | 学习时长排名、打卡激励 |
| 课表分享 | 生成课表图片分享 |
| 深色模式全局适配 | 所有Tab支持暗夜主题 |

---

## 8. 交互亮点总结

1. **课表+Todo+DDL一屏化**：首页看完课表直接看当天任务，零切换成本
2. **括号式极简Todo**：点击括号即完成，直观有趣味
3. **红绿双点月历**：绿点小=待办、红点大=截止，视觉层级一眼分明
4. **月历直接显示DDL名称**：不用点进去就知道是什么Deadline
5. **6格番茄钟滑窗**：一屏看6个，左右滑动顺手选择
6. **延迟聊天机制**：专注时不打扰，想聊再展开，兼顾陪伴感和专注力
7. **4种主题随心换**：适配不同时间、不同心情的学习氛围
8. **线上自习室室主权**：创建者可自定义背景，打造专属学习空间

---

> 课管助手 PRD v2.0 · 2026-08-23
>
> 配套交互原型：prototype.html
